# Deferred Queue — Pre-Launch

Items that could not be checked or completed in the section that surfaced them.
**Phase B sections and the final report MUST consume this file.** Every item below
has to reappear in `FINAL.md` marked resolved or still open — silence is a miss.

| # | From | Item | Status | What unblocks it |
|---|------|------|--------|------------------|
| D-1 | A1 (A1-4 / PF-10) | **`og-image.jpg`** | ✅ RESOLVED 2026-08-05 — built from the brand tokens (navy, orange rule, sky highlight, Sofia Pro, light logo), 1200×630 JPG at `public/og-image.jpg`. Source template kept at `src/assets/og-image-source.html` so it can be re-rendered. Originally: | Client must supply a 1200×630 JPG social share image. It is referenced three times in `src/layouts/BaseLayout.astro` — line 27 (schema.org `image`), line 91 (`og:image`), line 98 (`twitter:image`) — so until it lands, every Facebook/LinkedIn/X share of any page renders with no preview image. Drop the file at `public/og-image.jpg`; no code change needed. **Re-verify in B2** (social preview debuggers) and via `npm run preflight` PF-10. |
| D-2 | A1 (A1-4 / PF-17) | **No `geo` coordinates in the LocalBusiness JSON-LD** | ⏸ BLOCKED | Needs the client's real latitude/longitude — coordinates must not be invented. There is no GBP link to derive them from either: `src/data/constants.ts:17` is `googleBusiness: "#"`. Ask the client for (a) the Google Business Profile URL and (b) the service-area centroid they want to rank around. Because the practice publishes no street address (deliberate, see below), the correct schema shape is `geo` + `areaServed` without a `PostalAddress`. **Re-verify in B2** (NAP vs. GBP) and via preflight PF-17. |
| D-3 | A1 (A1-3 / PF-14) | **`/advisory-events` heading removed from `home-services-advisory-program.md`** | ⚠️ NEEDS SIGN-OFF | The line `### [You can view all of the monthly workshops for 2025 here.](/advisory-events)` linked to a page that was never migrated, and no workshops content exists anywhere in `src/`. It was removed to clear the PF-14 error. **If the client intends to publish an advisory/events page, this heading must be restored and pointed at the new route.** Confirm with the client, then restore or leave removed. |
| D-4 | A1 (A1-3 / PF-14) | **4 migrated blog links retargeted** | ⚠️ NEEDS SIGN-OFF | Dead WordPress URLs repointed at live equivalents: `/accounting-for-construction` → `/construction-accountants/`; `/blog/what-can-you-claim-with-the-r-d-tax-credit-for-construction-companies` → `/blog/construction-company-r-d-tax-credit/`; and two self-links in `construction-chart-of-accounts-2025.md` (`#` and `#chart`) → `/downloads/Atlas-Standard-Chart-of-Accounts.xlsx`. All four are judgment calls about author intent. Full before/after table in `reports/pre-launch/A1.md`. Human review to confirm the destinations match what each post meant to link to. |
| D-5 | A1 (A1-4 / PF-6) | **Brand-suffix decision** | ✅ RESOLVED 2026-08-05 — see D-13. Originally: | Not a per-page problem: 45 of the 62 would fall under 60 chars if the `" \| Atlas Accounting Group"` suffix (25 chars) were dropped or shortened; 17 would still be over. This is one editorial/SEO decision for the client, best asked as a single question. A2 owns title/description wording against the signed-off keyword map. |
| D-6 | A1 (A1-4 / PF-7) | **48 descriptions outside 150–160 chars** | ⚠️ OPEN, → A2 | Mostly over-length (170–363 chars) migrated WordPress excerpts. Two of the "too short" ones are noise — `/brand-guide/` and `/thank-you/` are `noindex: true` and never hit a SERP. `/profit-margin-calculator/` (85 chars) is indexed and genuinely thin. A2 to judge. |
| D-7 | A1 (observation) | **Empty anchor `[](/getting-started/)`** | ⚠️ OPEN, → A3 | `src/content/blog/home-services-advisory-program.md:128`. A link with no text has no accessible name. It resolves, so no preflight check catches it. Belongs to A3 (content/a11y). |
| D-8 | A4 (A4-6) | **`googleAnalyticsId` is empty — no GA4 Measurement ID supplied** | ⏸ BLOCKED | The team must provide the GA4 Measurement ID (`G-XXXXXXXXXX`) and it must be set at `src/data/constants.ts:55`. This is *not* a decision to decline analytics — nobody has decided either way, so the empty string must not be read as a decline (a comment at `src/data/constants.ts:50-54` now says so). While empty, `src/layouts/BaseLayout.astro:104` suppresses the gtag block entirely and the site ships with **zero analytics** — launching in this state means no traffic data from day one. The CSP already allows `www.googletagmanager.com` and `*.google-analytics.com`, so no header change is needed when the ID lands. **Re-verify in B2** (GA real-time hits). If the client genuinely declines analytics, record that decision here instead. |
| D-9 | A4 (A4-5) | **Tightened CSP needs live console confirmation** | ⚠️ NEEDS SIGN-OFF, → B1 | 6 unused origins plus `'unsafe-eval'` and `blob:` were removed from the CSP (`public/_headers:28`); full before/after and per-origin justification in `reports/pre-launch/A4.md`. Two removals cannot be fully proven from source: (a) `connect-src` still has no Calendly origin — Calendly's `widget.js` builds URLs at runtime, so if B1's console shows a `connect-src` violation, add `https://calendly.com https://assets.calendly.com`; (b) `img-src` no longer allows the blanket `https:` — correct today (zero remote images in source or build), but a future blog post embedding a remote image would be silently blocked. **In B1, load a page with the Calendly embed and check the console for `Refused to …` messages.** |
| D-10 | A4 (A4-5) | **`form-action` deliberately left unset** | ⚠️ OPEN | `default-src 'self'` does not cover `form-action`, so form-hijacking via injected markup is unblocked. `form-action 'self'` was *not* added because `SITE_DATA.forms.careers` is `""` pending a third-party LeadFlow endpoint (`src/data/constants.ts:26-30`) — the directive would silently break that form the moment the endpoint is set. **Decide this together with the careers endpoint:** `form-action 'self'` if the handler stays same-origin, `form-action 'self' https://<handler>` otherwise. Ties to the careers-endpoint question in `CLIENT-QUESTIONS.md`. |
| D-11 | A4 (A4-2) | **`astro` bumped 7.0.3 → 7.1.6 — needs a visual smoke check** | ⚠️ NEEDS SIGN-OFF | Forced by four high-severity XSS advisories against astro `<=7.0.9` (GHSA-4g3v-8h47-v7g6, GHSA-f48w-9m4c-m7f5, GHSA-7pw4-f3q4-r2p2, GHSA-8mv7-9c27-98vc) plus the `sharp ^0.34.0` ceiling blocking the libvips fix. `npm audit` is now clean (0 vulnerabilities) and `npm run build` succeeds (73 pages), but the rendered output has not been visually diffed across the minor bump, and it landed while another agent had uncommitted work. **Spot-check a few pages in B3's responsive/Lighthouse pass.** Separately: astro is pinned exact (repo convention preserved), so future patch CVEs will not apply automatically — that pin needs periodic manual review. |

## Dispositioned in A4 — NOT blocked, recorded so they are not re-raised

- **`X-XSS-Protection` absent from `public/_headers`** — deliberate and correct. The header is deprecated and was removed on purpose; preflight flags its presence, not its absence. Do not reintroduce it.
- **ActiveCampaign form IDs in `functions/api/subscribe.js:40-42`** — public-by-design embed values, not secrets. The POST to `activehosted.com` happens server-side inside the Pages Function, which is why that origin is correctly absent from the CSP.
- **`BLOG_API_KEY` / `GITHUB_TOKEN` not set** — a Cloudflare Pages deployment-config task, not a repo security defect. `functions/api/blog.js:144` already fails closed when they are missing. Documented in `BLOG-API.md`. Owned by the deploy step, not by A4.
- **No `.env.example` in the repo** — not a gap. There is no local `.env` workflow; every secret is a Cloudflare Pages binding.

## Dispositioned in A1 — NOT blocked, recorded so they are not re-raised

- **No postal address in the schema (PF-17 WARN)** — deliberate. Remote-first, booking-driven practice; `src/data/constants.ts:6` documents it. Do not add an address.
- **`site-icon-192.png` / `site-icon-512.png` missing (part of the PF-10 WARN)** — not a gap. This project's PWA icons are `favicon-192.png` / `favicon-512.png` (present in `public/`), and `src/pages/site.webmanifest.ts:11-18` points at them on purpose with a comment saying so. PF-11 passes with 2 valid icons.
- **47 PF-20 keyword-alignment WARNs** — A2's work by design; `src/data/seo-map.mjs` keywords were signed off by the client on 2026-08-04 and no keyword was edited in A1.

## Appended by A2 (SEO alignment) — 2026-08-04

| # | From | Item | Status | What unblocks it |
|---|------|------|--------|------------------|
| D-12 | A2 (A2-3) | **4 genuine keyword near-duplicates need a client decision** | ⏸ BLOCKED | Zero *exact* duplicates (54 distinct keywords / 54 indexable rows, PF-20 clean), but four near-duplicate pairs target the same query and cannot be resolved by an agent — each changes the content strategy. **C4** `construction bookkeeping` (`/blog/construction-industry-bookkeeping/`) vs `construction bookkeeping tips` (`/blog/better-construction-bookkeeping/`) — "tips" appears nowhere on the second page; recommend retargeting it at `bookkeeping for plumbers` (302 impr, pos 29.3 — real, untargeted demand). **C9** `hvac cash flow` (`/blog/track-hvac-company-cash-flow/`) vs `hvac cash flow problems` (`/blog/profitable-hvac-cash-flow/`) — same invented-modifier pattern; merge or retarget. **C6** `weekly cash flow construction` (`/blog/cash-flow-for-construction/`) vs `construction cash flow forecast` (`/blog/cash-flow-forecast-for-construction-business/`) — 1,439 impr / 0 clicks at positions 40–57 across both; clearest merge candidate on the site. **C1** `labor burden calculator` (`/labor-calculator/`) vs `contractor labor cost calculator` (`/blog/contractor-labor-cost-calculator/`) — the tool page never uses the word "burden" and misses on all five elements. Full reasoning and proposed resolutions in `reports/pre-launch/A2.md` § A2-3. **Nothing was applied.** Until decided, 4 rows in the alignment matrix cannot converge (A2-4). |
| D-13 | A2 (A2-2 / PF-6) | **Brand-suffix decision** | ✅ RESOLVED 2026-08-05 — client chose: drop the suffix on blog posts, keep it on the homepage, service and utility pages. Implemented as a `brandSuffix` prop on BaseLayout (default true, false on `/blog/<slug>/`), with `og:site_name` added so Google can still render the site name. Over-length titles 62 → 26; preflight warnings 155 → 119. PF-6's "Page | Business" rule was narrowed to exempt blog posts (7th local patch). Originally: | D-5 handed this to A2; A2 confirms it is **one** question, not 62 edits. `src/layouts/BaseLayout.astro:81` appends a fixed 25-char `" \| Atlas Accounting Group"` to every `<title>`. **45 of the 62 over-length titles fall to ≤60 chars if that suffix is dropped**; 17 remain over (longest 85 chars). Shortening it to `" \| Atlas"` only clears 23. Ask the client one question: *may the brand suffix be dropped from `<title>`?* (it stays in H1, schema and OG tags). **Not changed unilaterally** — it affects every page. If yes, the edit is a one-line change in `BaseLayout.astro` plus a preflight re-run; the 17 stragglers then become genuine per-page edits. |
| D-14 | A2 (A2-2 / PF-7) | **26 migrated blog descriptions still 300–370 chars — deliberately NOT rewritten** | ⚠️ OPEN, needs client sign-off | Resolves the blog portion of D-6. These are auto-generated WordPress body excerpts, not written meta descriptions: they truncate mid-sentence in the SERP and several bury the target keyword past the cut-off. A2 rewrote only 4 (F-3) where the description was off-topic or below the floor **and** the page had little to lose. The rest were left because they sit on migrated content that earns real traffic — including the site's top three earners (`construction-chart-of-accounts-2025` 656 clicks, `construction-profit-and-loss-statement` 271, `average-profit-margin-for-construction-companies` 47). Rewriting them is a content project with real CTR upside (descriptions are not a ranking factor, so the risk is low and the upside is measurable) but it needs the client's voice and sign-off, ideally batched. Full list = the PF-7 WARN rows in `reports/pre-launch/preflight-report.md`. |
| D-15 | A2 (A2-2) | **2 migrated slugs do not match their keyword — recommend leaving** | ⚠️ OPEN, decision only | `/blog/construction-businesses-staffing/` targets `employee vs 1099 contractor construction` and shares **no** core term with its slug. `/blog/target-hvac-business-profit-margin/` targets `hvac profit margins` and differs only by singular/plural — this page has **148,106 impressions, the 2nd highest on the site**, and was deliberately *not* renamed: a 301 on that page to gain a plural is a bad trade. Recorded so nobody "fixes" it later. If the client ever wants either slug changed, it must ship with a 301 in `CUTOVER-301-PLAN.md` and be re-verified in B1. |

## Appended by A3 (content, schema & accessibility) — 2026-08-04

| # | From | Item | Status | What unblocks it |
|---|------|------|--------|------------------|
| D-16 | A3 (A3-6) | **Content Signals `ai-train`** | ✅ RESOLVED 2026-08-05 — client allows AI access AND training. `robots.txt` now emits `Content-Signal: search=yes, ai-input=yes, ai-train=yes`. Originally: | `robots.txt` (generated at `src/pages/robots.txt.ts`) emits no `Content-Signal:` line. The recommended posture for a lead-gen site is `Content-Signal: search=yes, ai-input=yes, ai-train=no`. **`search=yes` and `ai-input=yes` are settled** — both serve the visibility the site exists for, and `ai-input=no` would signal against the AI-citation outcome the client is paying for. **`ai-train` is not ours to decide**: it carries no visibility benefit and is a business/legal call about whether Atlas's content may be used to train models. Nothing was written to robots.txt; the recommendation, rationale and exact insertion point are recorded as a comment at `src/pages/robots.txt.ts:5-20`. **Ask Atlas one question: may your site content be used for AI model training?** Then add the directive inside the `User-agent: *` group and re-verify `GET /robots.txt` in B2. Caveat to set with the client: this is a Cloudflare-led IETF *draft* expressing a preference only — no technical enforcement, and Google has not committed to honouring it. If the zone ends up on Cloudflare's managed robots.txt, confirm which file is authoritative before editing. |
| D-17 | A3 (A3-5) | **llms.txt advertises a 404 for the homepage** | ⚠️ OPEN — upstream bug, not fixable in-repo | The homepage entry in `dist/llms.txt` is `https://www.myatlasaccountant.com/.md`, which does not exist — `astro-llms-md@2.2.2` strips `index` from the path and then appends `.md`. The real file is `dist/index.md`, and every *other* entry's URL is correct. Impact is small (one broken link in a file no major provider has committed to reading) but it is the homepage. Two options: add a `public/_redirects` rule `/.md /index.md 200` — **must be tested against a live Pages deploy in B1**, a leading-dot source path is unusual — or patch/upgrade the integration upstream. Not applied here because it cannot be verified from a local build. |
| D-18 | A3 (A3-5) | **P&L post still links an off-site Google Sheet as its only template** | ⚠️ OPEN, needs client decision | `src/content/blog/construction-profit-and-loss-statement.md:77` points at `docs.google.com/spreadsheets/d/1I0tz…/copy`. The post is the site's 3rd biggest click earner (271 clicks) with download-shaped intent, and a first-party file now exists at `/downloads/Atlas-Sample-PL-by-Class-or-Project.csv`. **The link was NOT swapped**: the CSV is a different fictional company (Goldilocks Heating and Cooling, Project XYZ, $104,447 revenue) from the one the post's body walks through ($166,089 revenue, $36,750 net income, 22% margin, January 2023) — swapping would make three paragraphs of copy factually wrong. A3 **added** the Atlas CSV as a clearly-labelled second resource instead. **Decide:** either (a) rebuild the CSV to match the post's worked example and retire the Google Sheet, or (b) rewrite the post's walkthrough around the Atlas numbers, or (c) keep both as-is. Also worth confirming who owns that Google account — if it is a departed employee's, the link dies with it. |
| D-19 | A3 (A3-1) | **30 alt-text rewrites need a brand-voice skim** | ⚠️ NEEDS SIGN-OFF | Every rewrite was made after opening the actual image file, and three fixed alts that were factually *wrong* (`/` and `/construction-accountants/` heroes described "an accountant meeting a contractor" for a photo of one man alone in a truck; the solar page described a software-logo collage as "accounting for construction team"; `/labor-calculator/` used "Bookkeeping for electricians" for a photo of a fibre patch rack). The rest replaced keyword restatements of the H1 with descriptions of the photo. Photo interpretation is subjective — full before/after table in `reports/pre-launch/A3.md` § "What changed". Separately: the site splits illustrative section photos between `alt="" aria-hidden="true"` and descriptive alt, page by page. **A3 deliberately did not re-litigate that split**, only made the text accurate. If the client wants all illustrative section photos treated as decorative, that is a separate decision. |
| D-20 | A3 (A3-3) | **`og:type` is `website` on all 51 blog posts** | ⚠️ OPEN, → A2 / B2 | `src/layouts/BaseLayout.astro:87` hardcodes `<meta property="og:type" content="website">` for every page. `article` is correct for blog posts and would let Facebook/LinkedIn render them as articles. **Not changed in A3** — it touches the shared layout and belongs with the A2 metadata work rather than being slipped into a schema pass. ~4 lines (a `pageType` prop on `BaseLayout`, set from `[...slug].astro`). Re-verify in B2 with the social preview debuggers. |
| D-21 | A3 (A3-6) | **70 generated `.md` twins are indexable** | ⚠️ OPEN, low priority, → B1 | `astro-llms-md` emits a `.md` copy of every public page (`/hvac-accounting.md`, `/blog/<slug>.md`, …). The AI-readiness companion checklist names this as a duplicate-content pitfall. A3 excluded the `noindex` routes (`/brand-guide/`, `/thank-you/`) from generation, but the twins of *indexable* pages still ship with no `X-Robots-Tag`. Fix is one rule in `public/_headers` — `/*.md` → `X-Robots-Tag: noindex` — which keeps agent access while removing them from search. **Not applied**: `public/_headers` is the file A4 audited, and the effect should be confirmed against live response headers in B1. |
| D-22 | A3 (A3-7) | **Two NAP drift risks — reported only (A3-7 is VERIFY ONLY)** | ⚠️ OPEN, safe today | Displayed NAP is fully consistent across the build (one phone format, one `tel:` href, one email, one business name, zero variants), so nothing is wrong right now. But: (1) `src/pages/getting-started.astro:52` hardcodes both the href and the visible number — `<a href="tel:3609000421">Call (360) 900-0421</a>` — the only such copy on the site; every other instance renders from `SITE_DATA`. If the number changes, this button silently keeps the old one. Should become `{SITE_DATA.company.phoneHref}` / `{SITE_DATA.company.phone}`. (2) `SITE_DATA.company.legalLine` (`src/data/constants.ts:4`) is **never read** — `src/components/Footer.astro:16` assembles the same sentence independently from `company.name` + a hardcoded `"Lewis Group CPAs"`. Either wire the footer to `legalLine` or delete the constant. Both are one-line changes; left alone because A3-7 is a verify-only item. |

### Resolved by A3

- **D-7 — empty anchor `[](/getting-started/)`** — ✅ **RESOLVED.** Removed from `src/content/blog/home-services-advisory-program.md:128`. A **second, previously untracked** empty anchor was found during the same sweep and fixed: `src/content/blog/4-ways-construction-project-tracking.md:62` was malformed markdown (`### **[** 📊 **Grab the dashboard here]**[](/toolbox/)`) that rendered a bracket-littered heading *plus* a separate `<a href="/toolbox/"></a>` with no accessible name. Now `### [📊 **Grab the dashboard here**](/toolbox/)`. Verified: `grep -rc '<a [^>]*></a>' dist --include="*.html"` returns nothing across all 73 pages.
